
public class Note extends Records {

    public Note(String message, int index) {
       super(message, index);
    }
    public Note(Long id, String message, int index) {
        super(id, message, index);
    }
}
